# =====================================================
# DOCWRANGLER UI ENVIRONMENT CONFIGURATION
# =====================================================
# This .env.local file is used by the TypeScript frontend code in DocWrangler.
# It controls UI features like the improve prompt feature and chatbot assistant.
# These are the models used to help you develop your pipeline, NOT the models
# that execute your actual DocETL pipelines.
#
# To use: Copy this file to .env.local and fill in your actual values.
# =====================================================

# Required: API key for UI assistant features
# This is used for the chatbot and improve prompt features in the DocWrangler UI
# 
# For OpenAI (default):
OPENAI_API_KEY=sk-xxx
OPENAI_API_BASE=https://api.openai.com/v1
MODEL_NAME=openai/gpt-4o-mini

# For other providers, comment out the above and use the appropriate variables:
# 
# Anthropic:
# ANTHROPIC_API_KEY=sk-ant-...
# MODEL_NAME=claude-3-haiku-20240307
#
# Google Gemini:
# GEMINI_API_KEY=...
# MODEL_NAME=gemini/gemini-pro
#
# See https://docs.litellm.ai/docs/providers for more providers and model names

# Backend connection settings
NEXT_PUBLIC_BACKEND_HOST=localhost
NEXT_PUBLIC_BACKEND_PORT=8000

# Set to true if using the hosted version at docetl.org
NEXT_PUBLIC_HOSTED_DOCWRANGLER=false